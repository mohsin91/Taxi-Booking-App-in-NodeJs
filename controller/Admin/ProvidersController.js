module.exports = function () {
  require('../../services/Admin/ProvidersService')()
  require('../../Utils/common')()
  require('dotenv').config()
  this.providerListViewCtrl = (req, callback) => {
    var response = {}
    this.providersListViewService(req, (result) => {
      if (result.error) {
        response.error = true
        response.msg = result.msg
      } else {
        response.error = false
        response.msg = result.msg
        response.data = result.data
      }
      callback(response)
    })
  }
  this.getProviderCtrl = (req, callback) => {
    var response = {}
    this.getProviderViewService(req, (result) => {
      if (result.error) {
        response.error = true
        response.msg = result.msg
      } else {
        response.error = false
        response.msg = result.msg
        response.data = result.data
      }
      callback(response)
    })
  }
  this.getProviderDocCtrl = (req, callback) => {
    var response = {}
    this.getProviderDocViewService(req, (result) => {
      if (result.error) {
        response.error = true
        response.msg = result.msg
      } else {
        response.error = false
        response.msg = result.msg
        response.data = result.data
      }
      callback(response)
    })
  }
  this.providerDocEditCtrl = (req, callback) => {
    var response = {}
    this.providerDocEditService(req, (result) => {
      if (result.error) {
        response.error = true
        response.msg = result.msg
      } else {
        response.error = false
        response.msg = result.msg
        response.data = result.data
      }
      callback(response)
    })
  }
  this.providerEditCtrl = (req, callback) => {
    var response = {}
    this.providerEditService(req, (result) => {
      if (result.error) {
        response.error = true
        response.msg = result.msg
      } else {
        response.error = false
        response.msg = result.msg
        response.data = result.data
      }
      callback(response)
    })
  }
  this.providersPushNotificationPageViewCtrl = (req, callback) => {
    var response = {}
    this.providersPushNotificationPageViewService(req, (result) => {
      if (result.error) {
        response.error = true
        response.msg = result.msg
      } else {
        response.error = false
        response.msg = result.msg
        response.data = result.data
      }
      callback(response)
    })
  }
  this.providerPushNotificationSearchDataViewCtrl = (req, callback) => {
    var response = {}
    this.providerPushNotificationSearchDataViewService(req, (result) => {
      if (result.error) {
        response.error = true
        response.msg = result.msg
      } else {
        response.error = false
        response.msg = result.msg
        response.data = result.data
      }
      callback(response)
    })
  }
  this.providersPushNotificationSendCtrl = (req, callback) => {
    var response = {}
    this.providersPushNotificationSendService(req, (result) => {
      if (result.error) {
        response.error = true
        response.msg = result.msg
      } else {
        response.error = false
        response.msg = result.msg
        response.data = result.data
      }
      callback(response)
    })
  }
  this.getProviderDetailsListViewCtrl = (req, callback) => {
    var response = {}
    this.getProviderDetailsListViewService(req, (result) => {
      if (result.error) {
        response.error = true
        response.msg = result.msg
      } else {
        response.error = false
        response.msg = result.msg
        response.data = result.data
      }
      callback(response)
    })
  }
  this.getProviderVehicleListViewCtrl = (req, callback) => {
    var response = {}
    this.getProviderVehicleListViewService(req, (result) => {
      if (result.error) {
        response.error = true
        response.msg = result.msg
      } else {
        response.error = false
        response.msg = result.msg
        response.data = result.data
      }
      callback(response)
    })
  }
  this.providerVehicleEditCtrl = (req, callback) => {
    var response = {}
    this.providerVehicleEditService(req, (result) => {
      if (result.error) {
        response.error = true
        response.msg = result.msg
      } else {
        response.error = false
        response.msg = result.msg
        response.data = result.data
      }
      callback(response)
    })
  }
  this.providerVehicleDocumentsEditCtrl = (req, callback) => {
    var response = {}
    this.providerVehicleDocumentsEditService(req, (result) => {
      if (result.error) {
        response.error = true
        response.msg = result.msg
      } else {
        response.error = false
        response.msg = result.msg
        response.data = result.data
      }
      callback(response)
    })
  }
  this.getProviderVehicleDocumentsDetailsListViewCtrl = (req, callback) => {
    var response = {}
    this.getProviderVehicleDocumentsDetailsListViewService(req, (result) => {
      if (result.error) {
        response.error = true
        response.msg = result.msg
      } else {
        response.error = false
        response.msg = result.msg
        response.data = result.data
      }
      callback(response)
    })
  }
}
