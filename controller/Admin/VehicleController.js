module.exports = function () {
  require('../../services/Admin/VehicleService')()
  require('../../Utils/common')()
  require('dotenv').config()

  this.vehicleBrandAddCtrl = (req, callback) => {
    var response = {}
    this.vehicleBrandAddService(req, (result) => {
      if (result.error) {
        response.error = true
        response.msg = result.msg
      } else {
        response.error = false
        response.msg = result.msg
        response.data = result.data
      }
      callback(response)
    })
  }
  this.vehicleBrandViewCtrl = (callback) => {
    var response = {}
    this.vehicleBrandViewService((result) => {
      if (result.error) {
        response.error = true
        response.msg = result.msg
      } else {
        response.error = false
        response.msg = result.msg
        response.data = result.data
      }
      callback(response)
    })
  }
  //vehicle Categories
  this.vehicleCategoriesCtrl = (callback) => {
    var response = {}
    this.vehicleCategoriesService((result) => {
      if (result.error) {
        response.error = true
        response.msg = result.msg
      } else {
        response.error = false
        response.msg = result.msg
        response.data = result.data
      }
      callback(response)
    })
  }
  this.vehicleBrandPageViewCtrl = (req, callback) => {
    var response = {}
    this.vehicleBrandPageViewService(req, (result) => {
      if (result.error) {
        response.error = true
        response.msg = result.msg
      } else {
        response.error = false
        response.msg = result.msg
        response.data = result.data
      }
      callback(response)
    })
  }
  this.getVehicleBrandViewCtrl = (req, callback) => {
    var response = {}
    this.getVehicleBrandViewService(req, (result) => {
      if (result.error) {
        response.error = true
        response.msg = result.msg
      } else {
        response.error = false
        response.msg = result.msg
        response.data = result.data
      }
      callback(response)
    })
  }
  this.vehicleBrandEditCtrl = (req, callback) => {
    var response = {}
    this.vehicleBrandEditService(req, (result) => {
      if (result.error) {
        response.error = true
        response.msg = result.msg
      } else {
        response.error = false
        response.msg = result.msg
        response.data = result.data
      }
      callback(response)
    })
  }
  this.vehicleModelAddCtrl = (req, callback) => {
    var response = {}
    this.vehicleModelAddService(req, (result) => {
      if (result.error) {
        response.error = true
        response.msg = result.msg
      } else {
        response.error = false
        response.msg = result.msg
        response.data = result.data
      }
      callback(response)
    })
  }
  this.vehicleModelPageViewCtrl = (req, callback) => {
    var response = {}
    this.vehicleModelPageViewService(req, (result) => {
      if (result.error) {
        response.error = true
        response.msg = result.msg
      } else {
        response.error = false
        response.msg = result.msg
        response.data = result.data
      }
      callback(response)
    })
  }
  this.vehicleModelViewCtrl = (callback) => {
    var response = {}
    this.vehicleModelViewService((result) => {
      if (result.error) {
        response.error = true
        response.msg = result.msg
      } else {
        response.error = false
        response.msg = result.msg
        response.data = result.data
      }
      callback(response)
    })
  }
  this.getVehicleModelViewCtrl = (req, callback) => {
    var response = {}
    this.getVehicleModelViewService(req, (result) => {
      if (result.error) {
        response.error = true
        response.msg = result.msg
      } else {
        response.error = false
        response.msg = result.msg
        response.data = result.data
      }
      callback(response)
    })
  }
  //
  this.getVehicleBrandModelCtrl = (req, callback) => {
    var response = {}
    this.getVehicleBrandModelService(req, (result) => {
      if (result.error) {
        response.error = true
        response.msg = result.msg
      } else {
        response.error = false
        response.msg = result.msg
        response.data = result.data
      }
      callback(response)
    })
  }
  //
  
  //
  this.getRideTypeCtrl = (callback) => {
    var response = {}
    this.getRideTypeService((result) => {
      if (result.error) {
        response.error = true
        response.msg = result.msg
      } else {
        response.error = false
        response.msg = result.msg
        response.data = result.data
      }
      callback(response)
    })
  }
  //
  this.vehicleModelEditCtrl = (req, callback) => {
    var response = {}
    this.vehicleModelEditService(req, (result) => {
      if (result.error) {
        response.error = true
        response.msg = result.msg
      } else {
        response.error = false
        response.msg = result.msg
        response.data = result.data
      }
      callback(response)
    })
  }
  //
  this.saveProviderVehicle = (req, callback) => {
    var response = {}
    this.saveProviderVehicleService(req, (result) => {
      if (result.error) {
        response.error = true
        response.msg = result.msg
      } else {
        response.error = false
        response.msg = result.msg
        response.data = result.data
      }
      callback(response)
    })
  }
}
